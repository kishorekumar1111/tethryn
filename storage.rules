rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/{allPaths=**} {
      // Users can upload their own files
      allow write: if request.auth != null && request.auth.uid == userId;
      // Allow public read for experiences (or restrict if needed)
      allow read: if true;
    }
  }
}
